# نشر التطبيق على الإنترنت (Vercel + Turso)

التطبيق يعمل في وضعين بلا أي تغيير في الشيفرة:

| | محلياً على جهازك | على الإنترنت |
|---|---|---|
| قاعدة البيانات | `school.db` بجانب البرنامج | Turso (نفس SQLite في السحابة) |
| كلمة المرور | لا شيء | مطلوبة |
| ما يحدّد الوضع | — | وجود المتغيّر `TURSO_DATABASE_URL` |

النسخة المحلية تبقى كما هي وتعمل بلا إنترنت. الإنترنت إضافة لا بديل.

---

## 1) أنشئ قاعدة Turso

من [turso.tech](https://turso.tech) سجّلي بحساب GitHub، ثم أنشئي قاعدة
(Database) واحصلي على شيئين:

- **رابط القاعدة**: `libsql://اسم-قاعدتك-حسابك.turso.io`
- **رمز الدخول** (Auth Token): نصّ طويل يبدأ بـ `ey…`


> الخطة المجانية تكفي هذا التطبيق بمراحل: 5 غيغابايت و500 مليون قراءة
> شهرياً. قاعدتك كلها أقل من ميغابايت واحد.

---

## 2) ارفعي بياناتك الحالية إليها

**هذه أهمّ خطوة** — كل أسماء المعلمات والمواد والفصول والإسنادات والجدول
المولَّد تنتقل كما هي.

في PowerShell داخل مجلّد البرنامج:

```powershell
$env:TURSO_DATABASE_URL = "libsql://اسم-قاعدتك.turso.io"
$env:TURSO_AUTH_TOKEN   = "ey..."

python push_to_turso.py           # معاينة: يقارن المحلي بالسحابي
python push_to_turso.py --push    # الرفع الفعلي
```

الأمر يحفظ نسخة احتياطية محلية أولاً، ثم ينسخ كل جدول، **ثم يقارن عدد
الصفوف في الجهتين ويطبع عيّنة من الأسماء** — فلا تعتمدي على «تم» وحدها،
انظري إلى جدول المقارنة.

للتنزيل من السحابة إلى ملف محلي في أي وقت (نسخة احتياطية):

```powershell
python push_to_turso.py --pull
```

---

## 3) ارفعي المشروع إلى GitHub

```bash
git init
git add .
git commit -m "نظام جدول المدرسة"
git branch -M main
git remote add origin https://github.com/<حسابك>/jadwal.git
git push -u origin main
```

> ملف `.gitignore` يمنع رفع `school.db` عمداً — فيه أسماء المعلمات
> وأرقام جوالاتهنّ، والمستودع قد يكون عاماً. بياناتك في Turso لا في GitHub.

---

## 4) اربطي المشروع بـ Vercel

من [vercel.com](https://vercel.com) ← **Add New → Project** ← اختاري
المستودع. Vercel يتعرّف على Flask تلقائياً (لأن `app.py` يعرّف `app`)
فلا تحتاجين ضبط أمر بناء.

قبل **Deploy** أضيفي ثلاثة متغيّرات في **Environment Variables**:

| المتغيّر | القيمة |
|---|---|
| `TURSO_DATABASE_URL` | `libsql://اسم-قاعدتك.turso.io` |
| `TURSO_AUTH_TOKEN` | الرمز الطويل |
| `APP_PASSWORD` | كلمة المرور التي تشاركينها مع الإدارة |

> **`APP_PASSWORD` ليست اختيارية هنا.** إن تُركت فارغة يعمل الموقع بلا
> تسجيل دخول إطلاقاً، وأسماء المعلمات وجداولهنّ متاحة لأي أحد يعرف
> الرابط. محلياً الفراغ مقبول لأن الوصول من جهازك وحده.

اختياري: `SECRET_KEY` بأي نصّ عشوائي طويل — يجعل جلسات الدخول تصمد بين
عمليات النشر بدل أن تنتهي مع كل تحديث.

ثم **Deploy**. بعد دقيقة يعطيك رابطاً مثل `jadwal.vercel.app`.

---

## 5) اضبطي منطقة الخادم (مهمّة للسرعة)

**Settings → Functions → Function Region** ← اختاري المنطقة نفسها التي
أنشأتِ فيها قاعدة Turso، ثم **Redeploy**.

كل صفحة تنفّذ عدّة استعلامات، وكل استعلام رحلة شبكة كاملة بين الخادم
وقاعدة البيانات. إن كان الاثنان في القارّة نفسها فالرحلة ~2 مللي ثانية؛
وإن فُصلا (خادم في أمريكا وقاعدة في الهند مثلاً) صارت ~120 مللي ثانية
— أي ستّون ضعفاً، ومع عشرة استعلامات في الصفحة يصير الفرق ثانية كاملة
من الانتظار في كل نقرة.

لمعرفة منطقة قاعدتك: لوحة Turso ← القاعدة ← تظهر المنطقة بجانب اسمها
(`bom` = مومباي، `fra` = فرانكفورت، `iad` = واشنطن…).

---

## ملاحظات تشغيلية

**مدّة التوليد.** ضبطنا `maxDuration` على 300 ثانية في `vercel.json`،
وتوليد جدولك يأخذ ~90 ثانية. إن أبطأ الخادم فقلّلي «السقف الزمني» و«عدد
المحاولات» من شاشة التوليد.

**السرعة.** كل استعلام رحلة شبكة، فالصفحات أبطأ قليلاً من المحلي. الإدراج
الجماعي (858 حصة عند التوليد) يُرسَل في دفعات لا صفّاً صفّاً، وهذا هو
الفرق بين ثانية وعدّة دقائق.

**تعديل الشيفرة.** أي `git push` إلى `main` ينشر تلقائياً.

**كلمة المرور.** لتغييرها: Vercel → Settings → Environment Variables →
عدّلي `APP_PASSWORD` ثم **Redeploy**.

**الرجوع للمحلي.** احذفي `TURSO_DATABASE_URL` من بيئتك المحلية (أو افتحي
نافذة طرفية جديدة) فيعود البرنامج إلى `school.db` فوراً.

---

## الأدوات المحلية

هذه تعمل على جهازك فقط ولا تُرفع إلى Vercel:

| الأداة | الغرض |
|---|---|
| `push_to_turso.py` | رفع/تنزيل البيانات بين جهازك وTurso |
| `import_pdf.py` | استخراج الإسنادات من ملف PDF |
| `make_logo.py` | تجهيز ملفات الشعار من صورة |
| `reset_data.py` | تفريغ البيانات للبدء من جديد |
| `doctor.py` | فحص سلامة النظام من الطرفية |
| `test_*.py` | الاختبارات |

## الاختبارات

```
python test_app.py        كل المسارات
python test_turso.py      مسار السحابة كاملاً (بلا حساب — على ملف محلي)
python test_deploy.py     كلمة المرور والخط وملفات النشر
python doctor.py          سلامة بياناتك
```
